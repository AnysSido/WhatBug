@model WhatBug.WebUI.ViewModels.GlobalPermissions._SetGlobalPermissionsViewModel

@{    
    <form method="post" asp-controller="GlobalPermissions" asp-action="SetGlobalPermissions">
        <input asp-for="@Model.UserId" />
        <div class="modal-dialog modal-xl">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Modify Global Permissions</h5>
                    <br />
                    <p>Username: @Model.Username</p>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <table class="table table-bordered" cellspacing="0">
                        <thead>
                            <tr>
                                <th>Granted</th>
                                <th>Name</th>
                                <th>Description</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.GrantablePermissions.Count; i++)
                            {
                                <tr>
                                    <td>
                                        <input asp-for="@Model.GrantablePermissions[i].Id" type="hidden" />
                                        <input asp-for="@Model.GrantablePermissions[i].IsGranted" />
                                    </td>
                                    <td>@Model.GrantablePermissions[i].Name</td>
                                    <td>@Model.GrantablePermissions[i].Description</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="modal-footer justify-content-between">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </form>    
}